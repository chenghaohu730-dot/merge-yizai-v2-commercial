# 场景占位说明

本机未安装 Cocos Creator，不能可靠生成 `.scene` 文件。请在安装 Cocos Creator 3.8 LTS 后：

1. 打开 `game/cocos-creator-v2`。
2. 按 `docs/v2-cocos/02_场景节点蓝图.md` 创建 Boot、Home、Game、Result、Task、Shop、Rank 场景。
3. 将 `assets/scripts` 中的组件挂到对应节点。
4. 保存场景后，Creator 会自动生成 `.scene` 和 `.meta` 文件。
