# 2026-06-15 按钮与下一个球槽位对齐记录

## 本轮目标

继续修正式游戏主界面，不改首页、不恢复旧图。用户确认球体大小可以保留，重点修复：

- 底部声音、暂停、重开按钮没有贴准控制台三个槽位。
- 右上角“下一个”小球在圆槽里位置漂移。

## 已修复

- 按运行时 `game_shell.png` 重新量槽位，不使用旧估算坐标。
- 底部三个按钮中心对齐到约 `x=200 / 375 / 550`，纵向中心对齐到约 `y=1195`。
- 按钮尺寸由 112 回收为 104，避免按钮自带金边盖住底座槽金边。
- 右上“下一个”面板左移下调，中心对齐素材圆槽约 `x=625, y=145`。
- 微信小游戏端 `game.js` 与网页试玩端 `src/main.ts` 同步更新。

## 验证结果

```text
npm run build         通过
npm run test:wechat   通过
npm run test:viewport 通过，6 个视口
```

## 关键截图

```text
tests/device-checks/viewport-smoke/user-window-align-check.png
tests/device-checks/viewport-smoke/desktop-web-playing.png
tests/device-checks/viewport-smoke/short-screen-playing.png
```
