# 微信小游戏工程与验证脚本记录

验证日期：2026-06-11

## 本轮新增内容

创建原生微信小游戏工程：

```text
game/wechat-minigame
```

该工程不依赖 Cocos，使用微信小游戏 Canvas + Matter.js。

## 已包含文件

```text
game/wechat-minigame/game.js
game/wechat-minigame/game.json
game/wechat-minigame/project.config.json
game/wechat-minigame/project.private.config.json
game/wechat-minigame/libs/matter.min.js
game/wechat-minigame/assets/faces/*.png
game/wechat-minigame/assets/ui/visual_direction_concept.png
```

## 当前微信小游戏能力

- 竖屏 Canvas。
- Matter.js 物理引擎。
- 11 级头像合成。
- 第 11 级为用户指定正式亿仔头像。
- 本地最高分保存。
- 开始页。
- 暂停页。
- 失败线。
- 结算页。
- 粒子反馈。

## 自动验证

新增命令：

```text
npm run validate
```

验证结果：

```text
ok: true
checks: 20
```

覆盖内容：

- 必要文件存在且非空。
- 三处头像目录均有 11 个 PNG。
- 第 11 级亿仔头像为 `512 x 512`。
- 第 11 级亿仔头像有 alpha 通道。
- 第 11 级亿仔头像四角透明。
- 微信工程 `compileType` 为 `game`。
- 微信工程当前使用 `touristappid`。
- 微信 `game.js` 创建 Canvas。
- 微信 `game.js` 初始化 Matter.js。
- 微信 `game.js` 引用最终亿仔头像资源。

## 微信开发者工具 CLI

已确认 CLI 可用：

```text
C:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat
```

`--help` 可正常返回。

尝试打开项目：

```text
cli.bat open --project D:\01_Codex源码项目\微信小游戏\game\wechat-minigame --disable-gpu
```

结果：

- 命令超时。
- 但系统中已出现 `微信开发者工具.exe` 和多个 `wechatdevtools.exe` 进程，说明工具实际被启动。

尝试预览：

```text
cli.bat preview --project D:\01_Codex源码项目\微信小游戏\game\wechat-minigame --qr-format terminal --compile-condition {}
```

结果：

- 命令超时。
- 暂未拿到二维码或编译报告。
- 推测需要开发者工具 UI 登录、授权或手动确认后 CLI 才能继续。

## 尚未完成

- 微信开发者工具内的真实模拟器运行截图。
- 真机扫码预览。
- 正式 AppID 替换。
- 上传体验版。

## 追加验证

2026-06-11 已补充移动端视口烟测：

```text
npm run test:viewport
```

结果：

- `iphone-12` 通过。
- `compact-android` 通过。
- 顶部栏“下一个”文字挤压问题已修复。
- 截图输出到 `tests/device-checks/viewport-smoke/`。

## 追加验证 2

2026-06-11 已补充微信运行时模拟烟测和分享检查：

```text
npm run test:wechat
```

结果：

- 初始化通过。
- Canvas 绘制通过。
- 触摸事件注册通过。
- 分享菜单注册通过。
- 分享文案回调通过。

`npm run validate` 已扩展到 `23` 项检查，并确认微信小游戏包体为 `2,949,664` bytes，小于 4MB。
