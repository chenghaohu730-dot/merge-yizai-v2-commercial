# 微信分享与运行时烟测记录

验证日期：2026-06-11

## 本轮新增内容

- 微信小游戏端加入 `wx.showShareMenu`。
- 微信小游戏端加入 `wx.onShareAppMessage`。
- 结算页加入“分享成绩”按钮。
- 新增微信运行时模拟烟测：

```text
npm run test:wechat
```

## 微信运行时烟测结果

```text
ok: true
frames: 10
drawOps: 1299
touchHandlers.start: 1
touchHandlers.move: 1
touchHandlers.end: 1
canvas: 780 x 1688
share.showShareMenu: 1
share.onShareAppMessage: 1
```

验证覆盖：

- `game/wechat-minigame/game.js` 可在模拟微信环境中初始化。
- Canvas 可创建并绘制。
- Matter.js 可加载。
- 触摸事件可注册。
- 开始按钮触摸路径不报错。
- 掉落触摸路径不报错。
- 暂停/继续路径不报错。
- 分享菜单注册成功。
- 分享文案返回成功。

## 全套自动验证

本轮执行：

```text
npm run validate
npm run test:wechat
npm run build
npm run test:viewport
node --check game/wechat-minigame/game.js
```

结果：

- `npm run validate` 通过，`23` 项检查 OK。
- 微信小游戏包体大小：`2,949,664` bytes，小于 4MB 主包限制。
- `npm run test:wechat` 通过。
- `npm run build` 通过。
- `npm run test:viewport` 通过，覆盖 `iphone-12` 和 `compact-android`。
- `game/wechat-minigame/game.js` 语法检查通过。

## 仍未完成

- 微信开发者工具 CLI 的 `islogin`、`preview` 命令仍会超时。
- 微信开发者工具模拟器内截图尚未取得。
- 真机扫码预览尚未完成。
- 正式 AppID 和体验版上传尚未完成。

