# 最新资源表对齐状态

日期：2026-06-13

来源：`docs/V2.0美术资源需求表_给GPT.md`

## 已对齐到草案中的规则

- 新增 `assets/data/asset_manifest.json`，记录首页坐标、主游戏坐标、Cocos 坐标转换、首包/分包、球体安全区、特效序列帧规范。
- 新增 `assets/scripts/config/LayoutConfig.ts`，集中写入 750x1334 设计坐标和 Cocos 中心坐标转换公式。
- 新增 `assets/data/resource-gap-report.json`，明确哪些资源仍缺，避免把旧参考资源说成最终 V2 美术。
- `GameArea.ts` 改为按最新资源表的 `physics_rect`、`warning_line_y`、`dropper_anchor` 做默认派生。

## 尚未完成

- 没有生成最终美术资源。
- 没有 `btn_sound_on_*` 和 `btn_sound_off_*` 两套最终按钮图。
- 没有暂停弹窗的最终 `pause_panel / btn_resume / btn_pause_home / btn_pause_restart` 资源。
- 没有验证头像主体是否满足 460px 安全圆、26px 透明边距。
- 没有实现微信运行时安全区、刘海屏和胶囊按钮避让。
- 没有 Cocos 场景和 Prefab，仍不是可运行版本。

## 旧项目影响

本次对齐只改 `game/cocos-creator-v2` 和 `docs/v2-cocos`，不改 `game/wechat-minigame`。
